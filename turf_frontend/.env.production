# This file is used when running: npm run build
# It gets baked into the JS bundle — do NOT put secrets here.
#
# On VPS, nginx proxies /api/* to the Node.js backend on the same domain.
# So the base URL is just the site domain itself.
VITE_API_URL=https://booking.upliftsportsarena.com
